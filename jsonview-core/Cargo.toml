[package]
name = "jsonview-core"
version = "0.1.0"
edition = "2021"
description = "Pure business-logic library for jsonview"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
directories = "5"
