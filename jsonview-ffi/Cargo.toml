[package]
name = "jsonview-ffi"
version = "0.1.0"
edition = "2021"
description = "C/Swift FFI staticlib wrapping jsonview-core"

[lib]
crate-type = ["staticlib"]

[dependencies]
jsonview-core = { path = "../jsonview-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
